discord.py>=2.3.0
python-dotenv>=1.0.0
anthropic>=0.40.0
# beautifulsoup4 wurde entfernt -- war nur für character_list.py's
# swgoh.gg-HTML-Scrape nötig, das mit der Umstellung auf comlink-basierte
# Charakterdaten (roster.fetch_unit_names(), siehe Chat-Verlauf) retired
# wurde.
# Async-Client für die selbst gehostete comlink-Instanz (roster.py). Version
# nach PyPI-Stand zum Zeitpunkt dieser Änderung -- vor dem Deploy prüfen, ob
# eine neuere Version die in roster.py angenommene get_player()-Signatur
# geändert hat.
swgoh-comlink>=1.11.0
# Für /tw_lookup bild:True -- rendert die Konter-Tabelle als PNG statt als
# Codeblock (siehe bot.py: render_lookup_image()). Braucht ein Font-Paket
# im Image, um nicht auf Pillows hässlichen Bitmap-Default zurückzufallen
# -- siehe Dockerfile (fonts-dejavu-core).
Pillow>=10.0.0